using System.IO;

namespace notf
{
    public class OpenTypeReader
    {
        public void Reader(Stream stream)
        {
            using (var input = new BinaryReader(stream))
            {
                
            }
        }
    }
}
